apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Release.Name }}-config
  namespace: {{ .Values.namespace }}
data:
  MONGO_URL: "mongodb://mongodb-svc:27017/vegetable_order_app"
  MONGO_INITDB_DATABASE: {{ .Values.MONGO_INITDB_DATABASE}}

